# PDF Translator Environment Variables
# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed

# OpenAI-compatible API settings (works with DeepSeek, OpenAI, Ollama, llama.cpp, etc.)
OPENAI_API_KEY=your-api-key-here
OPENAI_API_BASE=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-chat

# For local servers (e.g., llama.cpp, Ollama), you typically don't need an API key:
# OPENAI_API_BASE=http://localhost:8080/v1
# OPENAI_MODEL=default_model

# Web server settings (pdf-translator-web only)
# STATIC_DIR=/path/to/static
